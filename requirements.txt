# MCP SDK
mcp[cli]>=1.2.0

# VNStock library for Vietnamese financial data
vnstock3>=3.2.0

# Data processing
pandas>=2.0.0

# Portfolio optimization (v1.0.0+)
# ⚠️ BREAKING CHANGES:
# - Adds ~500MB to container size (~1.2GB → ~1.7GB / 41% increase)
# - Significantly increases build time due to native extension compilation
# - Pulls in heavy transitive dependencies: scipy, numpy, cvxpy, qdldl
# - Requires system build tools: gcc, g++, build-essential (already in Dockerfile)
# - Requires binary wheels for platform compatibility (ARM64/x86_64)
# - Increases memory requirements during portfolio optimization calculations
#
# These dependencies enable 3 new tools:
# - calculate_returns: Expected returns calculation (mean_historical_return)
# - optimize_portfolio: Mean-Variance Optimization (max Sharpe ratio)
# - full_portfolio_optimization: Multi-strategy optimization (max Sharpe, min vol, max utility)
pyportfolioopt>=1.5.6
osqp>=0.6.0  # Quadratic programming solver (required by pyportfolioopt)

IPython